# Client

Unity mobile game client for GulfRun.

**Status:** Sprint 15 — Race HUD, Gameplay UI & In-Race Experience complete. A production OnGUI Race HUD on `Gameplay.unity`: professional 3-2-1-GO countdown with punch scale, tick/GO SFX hooks, and GO slide-away; player chrome (live ordinal place with change punch, future-ready Lap, speed, shield, weapon slot with uses/cooldown/pickup glow, race coins, optional gems, race timer); active-effect duration bars with color-blind shape tags; nearby-trap warning indicator only (never auto-avoids); quick in-race emotes (😀😂😎👏💪❤️) over the character; race-progress minimap bar (local/opponents/finish); finish banner with fireworks + confetti + crowd/fanfare hooks; camera look-ahead, gentle vertical bob, impact-only shake; dust/speed-trail VFX ring buffer; gameplay audio director; Gulf gold+sand `RaceHudTheme` + `RaceHudConfig` ScriptableObject; responsive `HudLayoutScale`; `RaceHudDebugView` at `panelX: 4510` (FPS / Speed / Position / Weapon ID / Trap ID / Ping). New `GulfRun.Features.RaceHud` assembly (Core+Domain only) with seven Core.Services seams so Features never reference Features. No final Canvas/TMP art or audio clips yet (see Sprint 15 Report §9).

**Prior status:** Sprint 16 — Character Selection, Locker & Customization complete. A full Locker experience on top of Sprint 8–11 foundations: 12 unlocked launch characters with instant showroom preview; Country locked at account creation (Flag / Name / Country Ranking only); default national Traditional Outfit re-applied after character select; Locker categories for Outfits / Headwear / Glasses / Victory Poses / Emotes / Footstep Effects / Running Effects / Profile Frames / Titles; Common→Mythic rarity borders/glow/reward flash; Owned / Not Owned / Temporary / Permanent / Country filters plus Newest / Rarity sort and Name / Category / Country search; Permanent Gem purchases via existing unlock path and Temporary 2/3/7-day grants via existing `ICosmeticGrantService` seams (Daily Missions / Login Rewards / Battle Pass / Events); premium Gulf Majlis OnGUI showroom (carpet, palms, lanterns, coffee set, city window) with cinematic rotate / zoom / auto-focus; Idle / Run / DoubleJump / Victory / Defeat / Celebrate preview animations with breathing, blink, and transitions; instant equip/unequip with automatic `PlayerPrefs` loadout persistence (`ILoadoutRepository` / `SaveManager` — the project's second deliberate cross-restart exception after Sprint 14's Intro flag); Features.Character-local `CharacterTheme` (no MainMenuTheme reference); null-safe dressing-room audio; phone/tablet UI scaling; `CharacterDebugView` fields for Character / Outfit / Animation / Country / Temporary Timer. No final 3D showroom art/audio yet (see Sprint 16 Report §10).

**Prior status:** Sprint 14 — GulfRun Brand Intro complete. The official GulfRun animated Brand Intro: a 2.65-second `Boot → Intro → MainMenu` sequence — moving desert sand dunes and soft wind particles fade in, a falcon flies across the screen (wing-sound cue) then circles above the dunes, a palm tree silhouette slowly fades in on the left, the GulfRun logo fades in with a premium golden shine sweep (shimmer-sound cue) beneath the "GULFRUN" wordmark, then a smooth 0.35s fade-to-black hands off to the Main Menu. Skippable from the device's **second** launch onward via this project's first genuine cross-restart persistence (`SaveManager.HasSeenIntro`/`MarkIntroSeen()`, backed by `UnityEngine.PlayerPrefs` — a deliberate, narrow exception to this project's otherwise fully in-memory persistence). A real music crossfade — not a hard cut — into the Lobby, via a new `AudioManager.FadeMusicTo` per-frame lerp that `MainMenuBootstrapper` now also uses to fade its own music in. One new shared `Core.Branding.GulfRunBrandMark` drawing routine (Desert Dunes + Falcon + Palm Tree + a forward-motion chevron, inside a medallion frame so it stays "readable even as an App Icon") is the **single** official GulfRun logo, now placed everywhere this Unity client can reach it: the Intro's animated reveal, a static mark on the Loading screen, and small badges on the Main Menu Top Bar, Store header, and Battle Pass header (App Icon/Website/Social Media/Marketing Materials are outside this repo's scope — no texture/marketing pipeline exists here to wire the mark into). No final art/audio assets, no rotation primitive for angled wing/shine art, and no async Loading-scene transition flow yet (see Sprint 14 Report §8).

**Prior status:** Sprint 13 — Main Menu & Lobby Design complete. A complete, playable Main Menu/Lobby screen: an animated per-session background reusing Sprint 12's real `IMapContextProvider` (random launch map + random Morning/Sunset/Night, moving clouds/birds/swaying palm trees) so the lobby's backdrop and the bottom bar's "Current selected map" are always the same one resolved environment; a centered breathing player preview showing current Character/Outfit/Country/national flag; a Top Bar (Player Name/Level/League/World Rank/Country Rank/Coins/Gems/Settings/Notifications); a Left Menu (Friends/Clan/Leaderboard/Missions/Battle Pass/Mail — Clan and Mail show an honest "Coming Soon" toast, no such Feature exists yet) and Right Menu (Store/Characters/Customize/Inventory/Events/Championships); a large golden PLAY button with an animated pulsing glow and a small click-compress animation, driving Quick Match creation/cancel end-to-end and this project's **first-ever real scene transition** (`Boot → MainMenu` automatically on launch, `MainMenu → Gameplay` once the lobby is full); a Daily Missions widget (3 missions, progress bars, Claim); a dismissible Login Reward popup (bobbing reward, streak day, Claim); a rotating Event Banner fed by four independent existing systems (Login/Special Events, Championships/Country Events, Battle Pass season, Store Special Offers) through a new many-producer `IEventBannerSource` registry; a Social panel (Friends/Clan Online, Invite, Room Code); a Voice Chat widget (Muted/Open Mic/Push-to-Talk); a Settings panel with 4 live audio category volume sliders; floating gold-dust particles; and a shared "Modern Gulf Identity" theme (gold accents, sand neutrals, rounded-panel + gold-underline motif) + button-press-animation kit used identically by every clickable element. 11 new `Core.Services` seams (`ILocalProfileProvider`, `IDailyMissionsPreviewProvider`, `ILoginRewardStatusProvider`, `IFriendsSummaryProvider`, `IMatchLobbySummaryProvider`, `INotificationSummaryProvider`, `IEventBannerSource`/`EventBannerRegistry`, `IMenuScreenOpener`/`MenuScreenRouter`, plus an extended `IMapContextProvider`) let the new `Features.MainMenu` assembly — this project's first and only Feature-to-Feature-boundary exception, since a lobby composition root by definition needs to read every other Feature's summary data — depend on nothing but `Core`/`Domain`, exactly like every other Feature. No final art/audio/microphone-capture assets, no Clan/Mail Feature, and no Loading-scene transition flow yet (see Sprint 13 Report §13).

**Prior status:** Sprint 12 — Gulf Maps & Level Design complete. Six beautiful, Gulf-inspired launch race maps (Kuwait City, Riyadh, Dubai, Doha, Manama, Muscat) sharing one fair, reusable 11-section-type level-structure library (Flat Sections/Small Hills/Slopes/Bridges/Wood Platforms/Stone Platforms/Jump Platforms/Short Tunnels/Open Areas/Small Drops/Small Climbs), every prefab keeping the same continuous, unbroken flat ground collider Sprint 3 established so platforms are always a bonus route, never a blocker; a data-driven `MapEnvironmentManager` that rolls a brand-new weighted Map + Weather (Sunny/Cloudy/Light Wind/Dusty Sky, plus Rain/Fog/Sandstorm authored at weight 0 for future support) + Time of Day (Morning/Sunset/Night, lighting-only) + fresh Trap/Item-Box random seeds at the start of every match, so trap/item-box placement can never be memorized into one perfect route; background-only per-city landmarks (Kuwait Towers, Kingdom Tower, Burj Khalifa, Doha Corniche/Pearl Qatar, Bahrain World Trade Center, Sultan Qaboos Grand Mosque, etc.) and a `BackgroundElementFlags` data flag per map (Clouds/Birds/Palm Trees/Waving Flags/City Lights At Night/Traffic/Sea Waves) driving a generic, reusable `ParallaxLayer` component; a dedicated ambient-audio channel on `AudioManager` for per-city Day/Night ambience; and a `MapDebugView` showing Current Map/Weather/Time/Trap Seed/Item Box Seed. No final landmark/background art, no final ambient audio clips, and no player-facing map-vote screen yet (see Sprint 12 Report §13).

**Prior status:** Sprint 11 — Daily Missions, Login Rewards & Reward System complete. 3 randomly-assigned Daily Missions (from a 25-entry configurable pool spanning Finish Races/Win Races/Collect Coins/Open Item Boxes/Use Weapons/Avoid Traps/Perform Jumps/Reach Top 3/Login Today) that reset every 24 hours across Easy/Medium/Hard difficulty with automatic reward scaling (1.0×/1.5×/2.0×); a 7-day Login Streak (Coins → Coins → Battle Pass XP → Gems → Temporary Cosmetic → Coins+Gems → weighted Mystery Reward) that resets to Day 1 on a missed day, plus 5 authored Special Login Event calendars (Ramadan, Eid, National Days, Summer, Winter); 2/3/7-day Temporary Cosmetics with live countdown timers, automatic removal (and auto-unequip) on expiry, and a Store "Unlock Permanently" upsell showing remaining time + Gem price; a "never grant a temporary duplicate — fallback to Coins/Gems instead" rule when a player already permanently owns a reward's cosmetic; Battle Pass XP as a first-class reward type; 4 new Mission/Login/Temporary-Item Notifications wired into Sprint 9's existing notification queue; and a mock-but-swappable cloud-ready `IProgressionBackendService` abstraction. No final reward-popup art/animation/SFX assets, no real backend, and no networked player avatar yet (see Sprint 11 Report §14).

**Prior status:** Sprint 10 — Store, Economy & Battle Pass System complete. A 10-tab modern Store (Special Offers, Gems, Coins, Battle Pass, Characters, Outfits, Emotes, Victory Poses, Visual Effects, Profile Frames); 6 configurable Gem Packages with bonus Gems and a limited-offer example; 5 configurable Coin Packs; an 18-entry Store Item catalog spanning Characters/Outfits (Traditional/Sports/Club Kits/National Kits)/Emotes/Victory Animations/Visual Effects/Profile Frames, priced in Gems, Coins, or bundled Real Money; 7 Limited/Special Offer bundles (Ramadan, Eid, National Days, Summer, Winter, Anniversary, a GCC Regional Celebration); a 10-tier "Paid only" Premium Monthly Battle Pass ($9.99, Season 1: Desert Champions) covering Exclusive Outfits/Emotes/Victory Poses/Profile Frames/Effects/Coins/Gems/a Title; a full Purchase System (Purchase Confirmation, Purchase History, Restore Purchases, Transaction Validation, a 48-hour Refund Protection window); a Player Wallet screen (Coins/Gems/Owned Cosmetics/Battle Pass Status); an Inventory screen; 5 new Store/Economy Notifications wired into Sprint 9's existing notification queue; and a mock-but-swappable cloud-ready `IStoreBackendService` abstraction. No real payment gateway, no final art/audio assets, and no networked player avatar yet (see Sprint 10 Report §14).

**Prior status:** Sprint 9 — Online Ecosystem, Rankings, Friends & Championships complete. World/Gulf/Country/Weekly/Monthly/Seasonal leaderboards for all 8 launch countries; public Player Profiles (Nickname, Player ID, Country, Character/Outfit, League/Season/Trophies, World/Gulf/Country Rank, Wins/Top3/Win Rate/Best Time, Coins/Gems, Favourite Character) with clickable ranks that open the Leaderboard scrolled-to and highlighting the player; 5-state Online Status; a full Friend System (send/accept/reject/cancel/remove/block/invite/join-lobby/view-profile) addable from Leaderboard, Player Profile, and Search, plus a shared Nearby-Players panel covering Lobby/End-Match/Tournament-Rankings; Nickname/Player-ID/Country search; a permanent, append-only Hall of Fame; 8 Leagues (Bronze→Legend) with season promotion/relegation; 5 Championship cadences and 13 Country/seasonal Events (all 8 National Days + Ramadan/Eid/Summer/Winter/a regional-tournament example); a 10-category Reward catalog; full Player Statistics tracking; Notifications; and a mock-but-swappable cloud-ready `IOnlineBackendService` abstraction with leaderboard caching. No final art/audio assets and no networked player avatar yet (see Sprint 9 Report §14).

**Prior status:** Sprint 8 — Characters, Countries & Customization System complete. 12 playable characters, all unlocked from launch and freely switchable; a one-time, permanent Account Creation flow (Display Name + Country) whose Country auto-applies a free Traditional Outfit for all 8 launch nations (Kuwait, Saudi Arabia, UAE, Qatar, Bahrain, Oman, Iraq, Egypt) and never changes again; a Gem-funded Premium Cosmetics system spanning 10 slots (Outfit shipping content today, Hat/Glasses/Shoes/Accessory/Back Item/Trail/Pet/Victory Pose/Emote reserved for future content with zero code changes needed); an extended Idle/Run/Jump/DoubleJump/Fall/Win/Lose/Celebrate animation vocabulary on one shared Animator Controller; and full networking of Character/Country/Outfit/Cosmetics/Victory Pose. No final art/audio/animation assets and no networked player avatar yet (see Sprint 8 Report §11, §14).

**Engine:** Targets Unity **2022.3.62f1 LTS**. As of Sprint 3, this machine has **Unity Hub installed but no Editor version downloaded** (superseding earlier Sprint reports' assumption of an installed Editor) — see Sprint 1 Report open item. The approved [Technical Stack](../docs/02-architecture/TECHNICAL_STACK.md) targets **Unity 6 LTS** long-term.

**Owner:** Client Lead

## Rules

- All first-party content under `Assets/_Project/`
- Assembly definitions mandatory
- No trusted economy logic — server authority only
- Follow [Coding Standards](../docs/03-standards/CODING_STANDARDS.md) and [Unity Packages](../docs/04-engineering/UNITY_PACKAGES.md)

## Sprint 1 notes

- See [`docs/07-sprints/SPRINT-01-PROJECT-FOUNDATION.md`](../docs/07-sprints/SPRINT-01-PROJECT-FOUNDATION.md) for the full Sprint Report.
- The Unity Editor on this machine has no activated license, so the project could not be opened, package-resolved, or batch-mode-compiled through Unity itself. All files were hand-authored to Unity's on-disk project format and cross-checked (YAML structure validation; C# compiled offline against the installed Editor's own reference assemblies). **First real Editor open + package resolution is a required next step before Sprint 2.**

## Sprint 2 notes

- See [`docs/07-sprints/SPRINT-02-PLAYER-CONTROLLER-FOUNDATION.md`](../docs/07-sprints/SPRINT-02-PLAYER-CONTROLLER-FOUNDATION.md) for the full Sprint Report.
- Same licensing constraint as Sprint 1: `Player.prefab`, the Animator Controller, and the two new ScriptableObject config assets were all hand-authored to Unity's on-disk YAML format, then structurally validated and cross-reference-checked offline. **First real Editor open remains required** to resolve the Input System package, assign real placeholder animations, and run a Play Mode smoke test.

## Sprint 3 notes

- See [`docs/07-sprints/SPRINT-03-ENDLESS-RUNNER-CORE.md`](../docs/07-sprints/SPRINT-03-ENDLESS-RUNNER-CORE.md) for the full Sprint Report.
- No Unity Editor is installed on this machine at all (only Hub) — all 5 new prefabs, 8 new ScriptableObject config assets, and the `Gameplay.unity` scene changes were hand-authored to Unity's on-disk YAML format, then structurally validated and fileID/guid cross-reference-checked offline. All 58 project `.cs` files (Sprints 1–3) were compiled together offline against a hand-written UnityEngine API shim. **First real Editor open remains required** before Play Mode can be exercised.
- A `Player.prefab` instance was deliberately **not** dropped into `Gameplay.unity` yet (same reasoning as Sprint 2) — see Sprint 3 Report §12 item 1.

## Sprint 4 notes

- See [`docs/07-sprints/SPRINT-04-MULTIPLAYER-FOUNDATION.md`](../docs/07-sprints/SPRINT-04-MULTIPLAYER-FOUNDATION.md) for the full Sprint Report, and [`docs/adr/0001-multiplayer-transport-abstraction.md`](../docs/adr/0001-multiplayer-transport-abstraction.md) for the (Proposed) Netcode-abstraction ADR.
- Still no licensed Unity Editor on this machine (only Hub) — the new `NetworkSyncConfig.asset`, the new `GulfRun.Features.Multiplayer` assembly definition, and the `Boot.unity`/`Gameplay.unity` scene changes were hand-authored to Unity's on-disk YAML format, then structurally validated and fileID/guid cross-reference-checked offline. All 93 project `.cs` files (Sprints 1–4) were compiled together offline against a hand-written UnityEngine API shim. **First real Editor open remains required** before Play Mode can be exercised.
- `Boot.unity` gained its **first-ever** manager GameObject (`MultiplayerSystems`) — none of Sprint 1's ten `Core.Managers.*` singletons were previously placed in any scene; this sprint does not retroactively fix that for the other ten (out of scope) — see Sprint 4 Report §18 item 2.
- No real multiplayer transport exists yet — `LocalLoopbackTransport` (in-process, no sockets) is the default so the Lobby/Ready/Countdown/Spawn/Sync architecture is fully testable via `MultiplayerDebugView`'s buttons today; swapping in a real transport once ADR-0001 is ratified requires no gameplay-facing code changes.

## Sprint 5 notes

- See [`docs/07-sprints/SPRINT-05-WEAPONS-ITEM-BOXES-COMBAT.md`](../docs/07-sprints/SPRINT-05-WEAPONS-ITEM-BOXES-COMBAT.md) for the full Sprint Report.
- Still no licensed Unity Editor on this machine (only Hub) — the 10 new `WeaponDefinition` assets, `WeaponCatalogConfig.asset`, `SpawnCategoryConfig_ItemBox.asset`, `ItemBoxPrefab.prefab`, the new `GulfRun.Features.Weapons` assembly, and the `Boot.unity`/`Gameplay.unity`/`ChunkPrefab_Default.prefab` changes were hand-authored to Unity's on-disk YAML format, then structurally validated and fileID/guid cross-reference-checked offline. All 115 project `.cs` files (Sprints 1–5) were compiled together offline against a hand-written UnityEngine API shim, extended this sprint with `AudioClip`/`Sprite`/`AudioSource`/`Coroutine`/`WaitForSeconds` stubs and a `Collider2D` base-class fix. **First real Editor open remains required** before Play Mode can be exercised.
- Every weapon's `WeaponDefinition` has real icon/sound/particle/animation-trigger fields wired end-to-end, but all are currently unassigned placeholders (no final art/audio) — same "data slot now, asset later" policy as every previous sprint.
- Weapon pickup/use/hit is fully host-authoritative over the same `IMatchTransport` seam Sprint 4 introduced — no new network abstraction was added, only six new events/methods on the existing interface.

## Sprint 6 notes

- See [`docs/07-sprints/SPRINT-06-DYNAMIC-TRAP-SYSTEM.md`](../docs/07-sprints/SPRINT-06-DYNAMIC-TRAP-SYSTEM.md) for the full Sprint Report.
- Still no licensed Unity Editor on this machine (only Hub) — the 15 new `TrapDefinition` assets, `TrapCatalogConfig.asset`, `TrapPrefab.prefab`, the new `GulfRun.Features.Traps` assembly, and the `Boot.unity`/`Gameplay.unity` changes were hand-authored to Unity's on-disk YAML format, then structurally validated and fileID/guid cross-reference-checked offline. All 130 project `.cs` files (Sprints 1–6) were compiled together offline against a hand-written UnityEngine API shim, extended this sprint with a `SpriteRenderer` stub and missing `Vector2` operators. **First real Editor open remains required** before Play Mode can be exercised.
- Traps reuse the exact same `WeaponEffectFlags`/`PlayerStatusEffect` pipeline Sprint 5 built for weapons (generalized with a new `EffectSourceKind`), plus one new flag (`LateralPush`) — no parallel effect system was introduced.
- The game's runner has no lane-change axis today (single-line side-scroller), so "choosing another lane" as a trap-avoidance method and Wind Gust/Dust Tornado's "sideways push" are both honestly scoped: obstacle traps are avoided by jumping only, and `LateralPush` is implemented as a real forward-progress setback rather than a fake lane nudge — see Sprint 6 Report §1.2 and §12 item 1.
- Trap spawning/expiration is fully host-authoritative and independent of the Sprint 3/5 chunk `SpawnPoint` system — a persistent `TrapAuthority` runs its own randomized ~4–9s (difficulty-scaled) spawn timer and ~12–18s per-trap lifetime, broadcasting over the same `IMatchTransport` seam.

## Sprint 7 notes

- See [`docs/07-sprints/SPRINT-07-RACE-FINISH-RANKING-VICTORY-CEREMONY.md`](../docs/07-sprints/SPRINT-07-RACE-FINISH-RANKING-VICTORY-CEREMONY.md) for the full Sprint Report.
- Still no licensed Unity Editor on this machine (only Hub) — the new `RaceFinishConfig.asset`, the new `GulfRun.Features.RaceFinish` assembly, the new `PodiumCameraDirector` (`Features.CameraSystem`), and the `Boot.unity`/`Gameplay.unity` changes were hand-authored to Unity's on-disk YAML format, then structurally validated and fileID/guid cross-reference-checked offline. All 152 project `.cs` files (Sprints 1–7) were compiled together offline against a hand-written UnityEngine API shim, extended this sprint with `AudioSource.loop`/`.clip`, three `Color` swatches, `GUI.Box`, and `Mathf.Sin`. **First real Editor open remains required** before Play Mode can be exercised.
- The race does not end at first place — a persistent host-only `RaceFinishAuthority` tracks every participant until they finish or are eliminated (gap-based warning/countdown with hysteresis, plus a race-wide safety-net timeout), then computes the final ranking and reward for everyone exactly once.
- The Podium Ceremony (top 3 only, 4th place never shown), Reward Screen (animated counters, local-player-only by presentation-layer filtering), and Lobby Return (reuses the existing `MatchState.Waiting` broadcast — no new networking message needed) are all `OnGUI` placeholders per the project's established "functional now, UI Toolkit later" policy — same status as every prior sprint's UI.
- `EconomyManager` gained a real (in-memory, non-persistent) `Coins` wallet this sprint specifically so `RaceRewardApplier` has somewhere real to credit rewards; `AudioManager` gained a separate looping music source for victory music, independent of one-shot SFX.

### Sprint 7 addendum — Victory Ceremony enhancements

- Every finisher's national flag (`PlayerIdentity.Country`, a new `GulfCountry` enum covering GulfRun's six official-map GCC nations) now animates gently behind their podium position, resolved from a new `FlagCatalogConfig` catalog.
- The champion now gets a Golden Trophy label, a ceremony-only Gulf Bisht overlay (cosmetic text, auto-hidden the instant this client's own ceremony view moves past the Podium phase), golden confetti (a new pure, deterministic `ConfettiSimulation`), and a distinct one-shot "special victory music" fanfare on top of the existing looping victory music. 2nd/3rd place gained a celebration pulse animation using the same shared oscillation helper as the champion's bounce.
- **Individual skip, fixed:** skipping the ceremony now only ever changes what the skipping player sees (`RaceStandingsTracker.LocalDisplayPhase`) — it no longer force-advances the phase for every other player. The host's shared, synchronized ceremony clock only ever advances early once every currently connected participant has independently chosen to skip.
- All 159 project `.cs` files (up from 152) recompile cleanly offline; see [`docs/07-sprints/SPRINT-07-RACE-FINISH-RANKING-VICTORY-CEREMONY.md`](../docs/07-sprints/SPRINT-07-RACE-FINISH-RANKING-VICTORY-CEREMONY.md) §14 for the full addendum report.

## Sprint 8 notes

- See [`docs/07-sprints/SPRINT-08-CHARACTERS-COUNTRIES-CUSTOMIZATION.md`](../docs/07-sprints/SPRINT-08-CHARACTERS-COUNTRIES-CUSTOMIZATION.md) for the full Sprint Report.
- Still no licensed Unity Editor on this machine (only Hub) — the 12 new `CharacterDefinition` assets, `CharacterCatalogConfig.asset`, `CosmeticCatalogConfig.asset`, `CountryCatalogConfig.asset`, the new `GulfRun.Features.Character` assembly, and the `Boot.unity`/`PlayerAnimatorController.controller` changes were hand-authored to Unity's on-disk YAML format, then structurally validated and fileID/guid cross-reference-checked offline. All 179 project `.cs` files (Sprints 1–8) were compiled together offline against a hand-written UnityEngine API shim, extended this sprint with `GUI.TextField`. **First real Editor open remains required** before Play Mode can be exercised.
- `CharacterId`/`CosmeticId` are string-wrapped structs, not enums (unlike `WeaponId`/`TrapId`) — adding character #13 or cosmetic #21 is purely a new `ScriptableObject` asset, never a code change.
- Country is chosen exactly once, at Account Creation, and is then permanently immutable for the account — `SessionManager.SetLocalPlayerCountry` (Sprint 7's placeholder setter) was removed entirely; `SessionManager.LocalPlayerCountry` now always reads the locked `SaveManager` account.
- `Boot.unity` gained its first `CharacterSystems` GameObject, which also **wires in `SaveManager` and `EconomyManager` for the first time ever** (both existed since Sprint 1/7 but had never been placed in any scene — see Sprint 8 Report §12) since `PlayerLoadoutManager` is the first feature to depend on both at runtime. The other eight Sprint-1 `Core.Managers.*` singletons remain unwired (carried forward from Sprint 4 Report §18 item 2).
- `EconomyManager` gained a real (in-memory, non-persistent) `Gems` wallet this sprint, alongside Sprint 7's `Coins`, specifically to fund Premium Cosmetic unlocks.

## Sprint 9 notes

- See [`docs/07-sprints/SPRINT-09-ONLINE-ECOSYSTEM-RANKINGS-FRIENDS-CHAMPIONSHIPS.md`](../docs/07-sprints/SPRINT-09-ONLINE-ECOSYSTEM-RANKINGS-FRIENDS-CHAMPIONSHIPS.md) for the full Sprint Report.
- Still no licensed Unity Editor on this machine (only Hub) — the 4 new `Settings/Online/*CatalogConfig.asset` catalogs, the new `GulfRun.Features.Online` assembly, and the `Boot.unity`/`Gameplay.unity` changes were hand-authored to Unity's on-disk YAML format, then structurally validated and fileID/guid cross-reference-checked offline. All 228 project `.cs` files (Sprints 1–9) were compiled together offline against a hand-written UnityEngine API shim, extended this sprint with `TextArea`, `GUI.BeginScrollView`/`EndScrollView`, a `GUI.Button(Rect, string, GUIStyle)` overload, and `GUISkin.button`. **First real Editor open remains required** before Play Mode can be exercised.
- A new permanent, backend-facing `PlayerId` (string-wrapped struct, `GR-XXXXXX` format, mirroring `CharacterId`/`CosmeticId`) is minted once in `SaveManager.CreateAccount` alongside the existing Display Name + Country — every online system (Leaderboards, Profiles, Friends, Hall of Fame) keys off this id, never the transient per-session `PlayerIdentity.PlayerId` Sprint 4 introduced for matchmaking.
- The entire online/social layer sits behind one new `Core.Backend.IOnlineBackendService` interface, mocked in-memory by `LocalOnlineBackendService` with deterministic seeded data — the exact same "abstract the whole remote system behind one interface, swap the implementation later" shape ADR-0001 established for `IMatchTransport`, applied a second time.
- Four small `Core.Services` seams (`PlayerStatEventService`, `ILocalLoadoutProvider`, `FriendRequestBridge`) let `Features.Online` read data from `Features.Character`/`Features.Weapons`/`Features.Traps`/`Features.RaceFinish` — and let those four features report actions to `Features.Online` — with zero Feature-to-Feature references in either direction.
- `Boot.unity` gained a new `OnlineSystems` GameObject (the 8 persistent Online singletons); `Gameplay.unity` gained `OnlineUI` (5 scene-scoped `OnGUI` screens) and `OnlineDebug` (`panelX: 2260`, this project's rightmost debug panel to date).

## Sprint 10 notes

- See [`docs/07-sprints/SPRINT-10-STORE-ECONOMY-BATTLE-PASS.md`](../docs/07-sprints/SPRINT-10-STORE-ECONOMY-BATTLE-PASS.md) for the full Sprint Report.
- Still no licensed Unity Editor on this machine (only Hub) — the 5 new `Settings/Store/*CatalogConfig.asset` catalogs, the new `GulfRun.Features.Store` assembly, and the `Boot.unity`/`Gameplay.unity` changes were hand-authored to Unity's on-disk YAML format, then structurally validated and fileID/guid cross-reference-checked offline. All 254 project `.cs` files (Sprints 1–10) were compiled together offline against a hand-written UnityEngine API shim — **no new shim extensions were required this sprint**. **First real Editor open remains required** before Play Mode can be exercised.
- Two small `Core.Services` seams (`ICosmeticGrantService`, `StoreNotificationBridge`) let `Features.Store` grant real Outfit/Emote/Victory-Pose cosmetics into `Features.Character`'s inventory and raise real notifications through Sprint 9's `NotificationManager` — with zero Feature-to-Feature references in either direction, the same shape `ILocalLoadoutProvider`/`FriendRequestBridge` established in Sprint 9.
- The entire Store/Battle Pass layer sits behind one new `Core.Backend.IStoreBackendService` interface, mocked in-memory by `LocalStoreBackendService` — the same "abstract the whole remote system behind one interface, swap the implementation later" shape ADR-0001 established for `IMatchTransport` and Sprint 9 applied to `IOnlineBackendService`, applied a third time to monetization.
- Every Outfit/Emote/Victory-Pose Store Item reuses the exact same `CosmeticId` Sprint 8's direct in-Character-Menu Gem unlock already offers — the Store is a second, richer storefront in front of that catalog, not a competing definition of the item; Visual Effects and Profile Frames (no existing `CosmeticSlot`) are owned via a new generic `OwnedStoreItem` ledger inside `IStoreBackendService`.
- `EconomyManager` gained `TrySpendCoins` alongside the existing `TrySpendGems`/`AddCoins`/`AddGems`, so Store Items can be priced in either currency.
- `Boot.unity` gained a new `StoreSystems` GameObject (`StoreManager`, `BattlePassManager`, `InventoryManager`); `Gameplay.unity` gained `StoreUI` (`StoreView`, `PlayerWalletView`, `InventoryView`) and `StoreDebug` (`panelX: 2710`, this project's rightmost debug panel to date).

## Sprint 11 notes

- See [`docs/07-sprints/SPRINT-11-DAILY-MISSIONS-LOGIN-REWARDS.md`](../docs/07-sprints/SPRINT-11-DAILY-MISSIONS-LOGIN-REWARDS.md) for the full Sprint Report.
- Still no licensed Unity Editor on this machine (only Hub) — the 7 new `Settings/Progression/*.asset` catalogs, the new `GulfRun.Features.Progression` assembly, and the `Boot.unity`/`Gameplay.unity` changes were hand-authored to Unity's on-disk YAML format, then structurally validated and fileID/guid cross-reference-checked offline. All 280 project `.cs` files (Sprints 1–11) were compiled together offline against a hand-written UnityEngine API shim — **no new shim extensions were required this sprint** (two small compile fixes were needed instead: a missing `using UnityEngine;` in the new `TrapAvoidanceTracker`, and a definite-assignment fix in `StoreView`). **First real Editor open remains required** before Play Mode can be exercised.
- Three small `Core.Services`/`Core.Backend` seams (`IBattlePassXpGrantService`, `ProgressionNotificationBridge`, extended `ICosmeticGrantService`) let `Features.Progression` grant Battle Pass XP, raise real notifications, and grant/query temporary cosmetic ownership — all without ever referencing `Features.Store`/`Features.Character` directly, the same shape `ICosmeticGrantService`/`StoreNotificationBridge` established in Sprint 10.
- The entire Progression layer sits behind one new `Core.Backend.IProgressionBackendService` interface, mocked in-memory by `LocalProgressionBackendService` — the same "abstract the whole remote system behind one interface, swap the implementation later" shape ADR-0001 established for `IMatchTransport` and Sprints 9–10 applied to `IOnlineBackendService`/`IStoreBackendService`, applied a fourth time.
- `CosmeticInventory` gained a parallel temporary-ownership map alongside its existing permanent one; `PlayerLoadoutManager` now auto-unequips and removes any temporary cosmetic the instant it expires, and `Features.Store`'s `StoreManager`/`StoreView` were extended (not duplicated) to show a "Temporary: Xh Ym left" countdown and an "Unlock Permanently" buy-button label for temporarily-owned Store items.
- Two brief mission types had no existing observation point: `WeaponInventoryManager` now raises `PlayerStatEventService.LocalItemBoxOpened` on every confirmed local pickup, and a new scene-scoped `TrapAvoidanceTracker` (added onto `Gameplay.unity`'s existing `TrapSystems` GameObject) raises `LocalTrapAvoided` for every trap that expires without hitting the local player.
- `Boot.unity` gained a new `ProgressionSystems` GameObject (`MissionManager`, `LoginRewardManager`, `TemporaryCosmeticExpiryWatcher`); `Gameplay.unity` gained `ProgressionUI` (`MissionsView` at `x: 1410`, `LoginRewardView` at `x: 1590`) and `ProgressionDebug` (`panelX: 3160`, this project's rightmost debug panel to date).

## Sprint 12 notes

- See [`docs/07-sprints/SPRINT-12-GULF-MAPS-LEVEL-DESIGN.md`](../docs/07-sprints/SPRINT-12-GULF-MAPS-LEVEL-DESIGN.md) for the full Sprint Report.
- Still no licensed Unity Editor on this machine (only Hub) — the 3 new `Settings/Maps/*CatalogConfig.asset` catalogs, the new `GulfRun.Features.Maps` assembly, 10 new chunk prefabs, and the `Boot.unity`/`Gameplay.unity`/`WorldGenerationConfig.asset`/`ChunkPrefab_Default.prefab` changes were hand-authored to Unity's on-disk YAML format, then structurally validated and fileID/guid cross-reference-checked offline. All 298 project `.cs` files (Sprints 1–12) were compiled together offline against a hand-written UnityEngine API shim, extended this sprint with a `RenderSettings` static class and a 4-argument `Color` constructor — the first sprint to touch scene ambient lighting/fog. **First real Editor open remains required** before Play Mode can be exercised.
- One small `Core.Services` seam (`IMapContextProvider`/`MapContextService`) lets `Features.EndlessRunner`'s `TrapAuthority`/`ChunkContentSpawner` re-seed their random spawn logic from the freshly-resolved per-match Trap/Item-Box seeds without ever referencing `Features.Maps` — the same shape `ICosmeticGrantService`/`IBattlePassXpGrantService` established in Sprints 9–11, applied a fifth time.
- Every launch map shares the exact same reusable 11-section-type chunk-prefab library (`WorldGenerationConfig.asset`, now 11 weighted entries instead of Sprint 3's 1) — per-map identity is presentation only (landmarks/palette/audio/lighting), never track geometry, which is what keeps all six maps fair and balanced for free.
- Every new Wood/Stone/Jump Platform prefab keeps the identical continuous flat ground `BoxCollider2D` every chunk has had since Sprint 3 — platforms are real, jumpable `BoxCollider2D`s positioned above that same floor, always a bonus route (reward for taking them) with a ground-level alternative directly beneath, never a gap that could unfairly block a runner.
- `Boot.unity` gained a new `MapSystems` GameObject (the persistent `MapEnvironmentManager`); `Gameplay.unity` gained its own scene-scoped `MapSystems` GameObject (`MapAmbientAudioController`, `BackgroundAmbienceController`, `MapDebugView` at `panelX: 3610`, this project's rightmost debug panel to date).
- No final landmark/background art, no `ParallaxLayer` instances placed in `Gameplay.unity` yet, and no final per-city ambient audio clips — same "data slot now, asset later" policy as every prior sprint's cosmetics/landmarks/audio.

## Sprint 13 notes

- See [`docs/07-sprints/SPRINT-13-MAIN-MENU-LOBBY.md`](../docs/07-sprints/SPRINT-13-MAIN-MENU-LOBBY.md) for the full Sprint Report.
- Still no licensed Unity Editor on this machine (only Hub) — the new `GulfRun.Features.MainMenu` assembly (17 new scripts) and the `Boot.unity`/`MainMenu.unity` changes were hand-authored to Unity's on-disk YAML format, then structurally validated and fileID/guid cross-reference-checked offline. All 340 project `.cs` files (Sprints 1–13) were compiled together offline against a hand-written UnityEngine API shim, extended this sprint with a minimal `UnityEngine.SceneManagement.SceneManager`/`Scene` stub (the first sprint to touch scene loading), `GUIStyle.wordWrap`, `GUI.HorizontalSlider`, `GUI.Toggle`, and `Mathf.Approximately`. **First real Editor open remains required** before Play Mode can be exercised.
- 11 new `Core.Services` seams let the new `Features.MainMenu` assembly — deliberately the **one** documented exception to "Features never reference other Features," since a lobby composition root by definition needs to read every other Feature's summary data — depend on nothing but `Core`/`Domain`, the same static-locator shape every prior cross-feature seam in this project already uses, applied at composition-root scale instead of one Feature pair at a time.
- This is the **first sprint to make any scene load another scene**: `SceneManager` (a `Singleton` stub since Sprint 1) gained real `LoadMainMenu()`/`LoadGameplay()` bodies, and `GameManager` (also a Sprint 1 stub) now calls `LoadMainMenu()` exactly once on boot. `Boot.unity` also gained its first-ever `CoreSystems` GameObject, finally placing `AudioManager`/`GameManager`/`UIManager`/`SceneManager` (all Sprint-1 `Singleton` stubs that had never been instantiated in any scene) plus the new `SettingsManager`; `OnlineSystems` gained `ProfileManager` (existed since Sprint 9, never previously placed in a scene either) — the same category of "wire up a long-dormant Sprint 1 stub" fix Sprint 8's report made for `SaveManager`/`EconomyManager`.
- Because every existing `Singleton<T>` manager already calls `DontDestroyOnLoad` on its **whole GameObject** (Sprint 1's base class), every persistent system — plus `CharacterMenuView`, a plain `MonoBehaviour` that happens to share `Boot.unity`'s `CharacterSystems` GameObject with three `Singleton`s — survives the Boot→MainMenu transition for free; `MainMenu.unity` additionally gets its own fresh, scene-local instances of `FriendListView`/`LeaderboardView`/`NotificationView`/`MissionsView`/`StoreView`/`InventoryView` plus two brand-new reused screens (`ChampionshipsView`, `BattlePassView`) so the lobby is fully self-sufficient without depending on `Gameplay.unity` ever having loaded first.
- `AudioManager` gained Master/Music/SFX/Ambient category volumes (every source's live volume is always `requested × category × master`, so a Settings-panel slider takes effect immediately with zero per-call-site math at any existing `PlayOneShot`/`PlayMusic`/`PlayAmbient` call site); a new `SettingsManager` owns those four volumes plus Voice Chat Mode in memory (no persistence yet).
- `Boot.unity` gained the new `CoreSystems` GameObject (root order 10, `AudioManager`/`GameManager`/`UIManager`/`SceneManager`/`SettingsManager`) and a 9th `OnlineSystems` component (`ProfileManager`); `MainMenu.unity` (previously a stub scene) gained `MainMenuUI` (14 components) and `MainMenuScreens` (9 components, including `MainMenuDebugView` at `panelX: 4060`, this project's rightmost debug panel to date, since Boot's persistent `CharacterDebugView` at `panelX: 10` is also visible in this scene).

## Sprint 15 notes

- See [`docs/07-sprints/SPRINT-15-RACE-HUD-GAMEPLAY-UI.md`](../docs/07-sprints/SPRINT-15-RACE-HUD-GAMEPLAY-UI.md) for the full Sprint Report.
- Still no licensed Unity Editor on this machine (only Hub) — the new `GulfRun.Features.RaceHud` assembly, `RaceHudConfig.asset`, and `Gameplay.unity` `RunnerHUD` wiring were hand-authored and offline-validated. All **394** project `.cs` files compiled together via `.compile_check` (0 errors / 0 warnings).
- Features never reference Features: seven new `Core.Services` seams feed the HUD from Countdown / Standings / Weapons / Status Effects / Trap proximity / Network diagnostics / Race timer.
- Legacy `CountdownView` remains on `RunnerHUD` but is disabled; `CountdownHudView` owns the production 3-2-1-GO presentation. `RaceHudDebugView` uses `panelX: 4510` (next free +450 after MainMenu `4060`).
- Concurrent Matchmaking / Character-Locker workstreams were deliberately left out of this commit.

## Sprint 16 notes

- See [`docs/07-sprints/SPRINT-16-CHARACTER-SELECTION-LOCKER.md`](../docs/07-sprints/SPRINT-16-CHARACTER-SELECTION-LOCKER.md) for the full Sprint Report.
- Still no licensed Unity Editor on this machine (only Hub) — `LockerView`, `LockerUiConfig.asset`, expanded `CosmeticCatalogConfig.asset`, and `Boot.unity` `CharacterSystems` wiring were hand-authored and offline-validated. All **394** project `.cs` files compiled together via `.compile_check` (0 errors / 0 warnings). Shim gained `PlayerPrefs` string APIs plus additional `Mathf`/`Time`/`Rect`/`GUIStyle` helpers needed by the Locker (and harmless for in-flight Race HUD / Traps code).
- Loadout persistence is a second narrow PlayerPrefs exception on `SaveManager` (`ILoadoutRepository`): Character + equipped cosmetics + permanent/temporary ownership survive app restarts; other progress fields remain in-memory.
- Main Menu Right Menu "Characters" opens the Locker on the Characters tab; "Customize" opens `MenuScreen.Locker` on Outfits. `CharacterTheme` keeps Features.Character free of Features.MainMenu references.
- Temporary cosmetics continue to flow through Sprint 11's `ICosmeticGrantService` (Missions / Login Rewards / Battle Pass / Events) — the Locker displays countdowns and respects auto-expiry/unequip already owned by `PlayerLoadoutManager`.

## Sprint 14 notes

- See [`docs/07-sprints/SPRINT-14-GULFRUN-BRAND-INTRO.md`](../docs/07-sprints/SPRINT-14-GULFRUN-BRAND-INTRO.md) for the full Sprint Report.
- Still no licensed Unity Editor on this machine (only Hub) — the new `Intro.unity` scene, the new `GulfRun.Features.Intro` assembly (6 new scripts), `Core.Branding.GulfRunBrandMark`, and the `Loading.unity`/`MainMenu.unity`/`EditorBuildSettings.asset` changes were hand-authored to Unity's on-disk YAML format, then structurally validated and fileID/guid cross-reference-checked offline. All 347 project `.cs` files (Sprints 1–14) were compiled together offline against a hand-written UnityEngine API shim, extended this sprint with a minimal in-memory `UnityEngine.PlayerPrefs` stub (`GetInt`/`SetInt`/`HasKey`/`Save` — the first sprint to touch persistence that must survive a real app restart) and `Mathf.PI`. **First real Editor open remains required** before Play Mode can be exercised.
- This is the project's **first genuine cross-restart persistence**: every other manager in this codebase (`SaveManager` included) is deliberately in-memory-only per the project's mock-but-swappable backend policy, but "has this device ever launched the game before" cannot honestly be answered that way, so `SaveManager.HasSeenIntro`/`MarkIntroSeen()` is a narrow, documented exception backed by `UnityEngine.PlayerPrefs`.
- `AudioManager` gained a real per-frame music crossfade (`FadeMusicTo` + a new `Update()` loop lerping the *requested* music volume) — the first time this project's audio layer does anything on a per-frame clock rather than only reacting to discrete Play/Stop calls; `IntroSequenceController` fades the Intro's music out to silence right as it fades to black, and `MainMenuBootstrapper` now starts its lobby music at 0 and fades it in, so the two calls compose into one continuous crossfade on the same persistent `AudioSource`.
- `GameManager`'s boot hand-off changed from `Boot → MainMenu` (Sprint 13) to `Boot → Intro → MainMenu`; `SceneManager` gained `LoadIntro()` alongside its existing `LoadMainMenu()`/`LoadGameplay()`. `EditorBuildSettings.asset`'s scene order is now `Boot → Intro → MainMenu → Loading → Gameplay → Results`.
- `Core.Branding.GulfRunBrandMark` is a new, deliberately Core-level (not Feature-level) static drawing routine — the single official GulfRun logo every screen must draw through, per the brief's "every future design must follow the GulfRun Brand Identity" rule — entirely `Rect`-driven so the same call composes a 28px Store-header badge or a full-screen animated Intro reveal with zero separate "compact" variant.
- `Intro.unity` (new scene) gained `IntroUI` (`IntroSequenceController`/`IntroBackgroundView`/`IntroFalconView`/`IntroLogoView`); `Loading.unity` (previously an empty Camera+Light stub) gained `LoadingUI` (`LoadingBrandView`); `TopBarView`/`StoreView`/`BattlePassView` each gained a small `GulfRunBrandMark` badge inline in their existing headers, no new GameObjects needed for those three.
- No final art/audio assets, no rotation primitive in this project's `OnGUI` drawing style (so the falcon's wings and the logo's shine sweep are flat, axis-aligned approximations rather than angled art), and App Icon/Website/Social Media/Marketing Materials remain outside this repo's scope (no texture/marketing-asset pipeline exists here) — same "data/logic now, final asset later" policy as every prior sprint.
